# Workflow: Разработка на Windows, развертывание на Ubuntu

Этот документ описывает типичный workflow для разработки бота на Windows с последующим развертыванием на сервере Ubuntu.

---

## Архитектура

```
┌─────────────────┐         ┌──────────┐         ┌─────────────────┐
│  Windows (Dev)  │ ────►   │  GitHub  │  ◄────  │  Ubuntu Server  │
│  Разработка     │  push   │          │  pull   │  Production     │
└─────────────────┘         └──────────┘         └─────────────────┘
```

---

## Настройка (один раз)

### 1. На Windows (локальный компьютер)

1. **Установите Git для Windows:**
   - Скачайте с https://git-scm.com/download/win
   - Установите с настройками по умолчанию

2. **Настройте Git (если еще не настроен):**
   ```bash
   git config --global user.name "Ваше имя"
   git config --global user.email "your.email@example.com"
   ```
   > **Примечание:** Это нужно сделать один раз. Git запомнит эти настройки для всех ваших проектов.

3. **Настройте Git репозиторий:**
   ```bash
   git_setup.bat
   ```
   > Скрипт автоматически проверит настройки Git и предложит их настроить, если они отсутствуют.
   
   Или вручную:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/FU_bot.git
   git push -u origin main
   ```

4. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Создайте новый репозиторий (Private рекомендуется)
   - Скопируйте URL

### 2. На Ubuntu сервере

1. **Подключитесь по SSH:**
   ```bash
   ssh user@your-server-ip
   ```

2. **Клонируйте репозиторий:**
   ```bash
   cd /opt
   sudo git clone https://github.com/dxmxrxtskx/tg_forus_bot.git
   cd tg_forus_bot
   sudo chown -R $USER:$USER .
   ```

3. **Установите зависимости (автоматически):**
   ```bash
   chmod +x setup_server.sh
   sudo ./setup_server.sh
   ```
   > Скрипт установит Docker, Docker Compose и все необходимые компоненты

4. **Настройте бота:**
   ```bash
   python3 deploy.py
   # Или создайте .env и config.json вручную
   ```

5. **Запустите бота:**
   ```bash
   docker compose up -d
   ```

6. **Сделайте скрипт обновления исполняемым:**
   ```bash
   chmod +x update.sh
   ```

---

## Ежедневный workflow

### Шаг 1: Разработка на Windows

1. Внесите изменения в код
2. Протестируйте локально (если нужно):
   ```bash
   python bot.py
   ```

3. Загрузите изменения на GitHub:
   ```bash
   git_push.bat "Описание ваших изменений"
   ```

   Или вручную:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```

### Шаг 2: Развертывание на Ubuntu сервере

1. **Подключитесь к серверу:**
   ```bash
   ssh user@your-server-ip
   ```

2. **Обновите код:**
   
   **Вариант A: Использовать скрипт (рекомендуется)**
   ```bash
   cd /opt/FU_bot
   ./update.sh
   ```
   
   **Вариант B: Вручную**
   ```bash
   cd /opt/FU_bot
   git pull
   docker compose restart
   ```
   
   **Вариант C: Полная пересборка (если изменились зависимости)**
   ```bash
   cd /opt/FU_bot
   git pull
   docker compose down
   docker compose build
   docker compose up -d
   ```

3. **Проверьте логи:**
   ```bash
   docker compose logs -f
   ```

---

## Типичные сценарии

### Добавление новой функции

1. **Windows:** Разработайте и протестируйте
2. **Windows:** `git_push.bat "Добавлена новая функция"`
3. **Ubuntu:** `ssh server` → `cd /opt/FU_bot` → `./update.sh`

### Исправление бага

1. **Windows:** Исправьте ошибку
2. **Windows:** `git_push.bat "Исправлен баг в обработке фильмов"`
3. **Ubuntu:** `ssh server` → `cd /opt/FU_bot` → `./update.sh`

### Обновление документации

1. **Windows:** Обновите README или другие документы
2. **Windows:** `git_push.bat "Обновлена документация"`
3. **Ubuntu:** Обновление не требуется (документация не влияет на работу бота)

### Изменение зависимостей

1. **Windows:** Обновите `requirements.txt`
2. **Windows:** `git_push.bat "Обновлены зависимости"`
3. **Ubuntu:** `ssh server` → `cd /opt/FU_bot` → `git pull` → `docker compose down` → `docker compose build` → `docker compose up -d`

---

## Полезные команды

### На Windows

| Действие | Команда |
|----------|---------|
| Загрузить изменения | `git_push.bat "Описание"` |
| Проверить статус | `git status` |
| Посмотреть историю | `git log --oneline` |
| Отменить локальные изменения | `git checkout -- .` |

### На Ubuntu сервере

| Действие | Команда |
|----------|---------|
| Обновить код | `./update.sh` или `git pull` |
| Перезапустить бота | `docker compose restart` |
| Посмотреть логи | `docker compose logs -f` |
| Проверить статус | `docker compose ps` |
| Остановить бота | `docker compose down` |
| Запустить бота | `docker compose up -d` |

---

## Решение проблем

### Проблема: "Permission denied" при git pull на сервере

**Решение:**
```bash
sudo chown -R $USER:$USER /opt/FU_bot
```

### Проблема: Конфликты при git pull

**Решение:**
```bash
# Сохраните локальные изменения (если есть)
git stash

# Получите обновления
git pull

# Примените сохраненные изменения
git stash pop

# Разрешите конфликты вручную, затем:
git add .
git commit -m "Resolve conflicts"
```

### Проблема: Бот не запускается после обновления

**Решение:**
```bash
# Проверьте логи
docker compose logs -f

# Попробуйте полную пересборку
docker compose down
docker compose build --no-cache
docker compose up -d
```

---

## Рекомендации

1. **Всегда делайте коммиты с понятными сообщениями**
   - ✅ Хорошо: `"Добавлена поддержка рейтингов для игр"`
   - ❌ Плохо: `"Изменения"`

2. **Тестируйте критичные изменения локально перед push**

3. **Используйте скрипт `update.sh` на сервере** для автоматизации

4. **Регулярно делайте резервные копии базы данных:**
   ```bash
   # На сервере
   cp data/multilists.db backups/multilists-$(date +%F).db
   ```

5. **Проверяйте логи после каждого обновления**

---

## Быстрая справка

**Разработка на Windows:**
```bash
git_push.bat "Описание изменений"
```

**Развертывание на Ubuntu:**
```bash
ssh user@server
cd /opt/FU_bot
./update.sh
```

Готово! 🚀

