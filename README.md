# Telegram Multi-List Bot

Telegram-бот для управления различными списками: фильмы, активности, поездки, тренды TikTok, фотографии, игры и другие категории.

Исходный код репозитория: [dxmxrxtskx/tg_forus_bot](https://github.com/dxmxrxtskx/tg_forus_bot).

## Возможности

### 🎬 Фильмы
- Списки ожидающих и просмотренных фильмов
- Категории: Фильмы, Сериалы, Мультики
- Топ-10 по рейтингам (общий и индивидуальный)
- Случайный выбор фильма
- Оценки от двух пользователей

### 📋 Активности
- Планируемые и выполненные активности
- Добавление, редактирование, удаление

### ✈️ Поездки
- Категории: Пешком, Поездки, Места в Херцег-Нови
- Возможность добавления новых категорий

### 📱 Тренды TikTok
- Список трендов для съемки
- Прикрепление видео
- Отметка выполненных трендов

### 📸 Фотографии
- Категории с описанием и ссылками
- Динамическое добавление категорий

### 🎮 Компьютерные игры
- Списки ожидающих и пройденных игр
- Топ-10 по рейтингам
- Случайный выбор игры
- Оценки от двух пользователей

### 🔞 Sexual
- Магазины по категориям
- Ссылки и описания

## Установка и развертывание

### Требования

- Python 3.11+
- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Git (для клонирования с GitHub)

### Автоматическое развертывание

1. Клонируйте репозиторий с GitHub:
   ```bash
   git clone https://github.com/dxmxrxtskx/tg_forus_bot.git
   cd tg_forus_bot
   ```

2. Запустите скрипт развертывания:
   ```bash
   python deploy.py
   ```

3. Скрипт запросит:
   - Токен бота
   - Telegram ID пользователей (минимум 2)
   - Отображаемые псевдонимы для каждого пользователя

4. После завершения скрипта запустите бота:
   ```bash
   docker-compose up -d
   ```

### Ручная установка

1. Создайте файл `.env`:
   ```
   BOT_TOKEN=your_bot_token_here
   ```

2. Создайте файл `config.json`:
   ```json
   {
     "users": [
       {
         "telegram_id": 123456789,
         "display_name": "Пользователь 1"
       },
       {
         "telegram_id": 987654321,
         "display_name": "Пользователь 2"
       }
     ]
   }
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Запустите бота:
   ```bash
   python bot.py
   ```

### Скрипты автоматизации (Ubuntu сервер)

- Первичная установка окружения (Docker, зависимости, перезапуск бота) выполняется скриптом:
  ```bash
  chmod +x setup_server.sh
  sudo ./setup_server.sh
  ```
  По умолчанию ожидается, что репозиторий находится в `/opt/tg_forus_bot`, но путь можно изменить перед запуском.

- Обновление работающего сервера из GitHub:
  ```bash
  chmod +x update.sh
  ./update.sh
  ```
  Скрипт выполняет `git pull`, пересобирает контейнер и перезапускает `docker compose` в текущей директории.

## Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Используйте кнопки меню для навигации

### Получение Telegram ID

Чтобы узнать свой Telegram ID, напишите боту [@userinfobot](https://t.me/userinfobot)

## Структура проекта

```
FU_bot/
├── bot.py              # Главный файл бота
├── config.py           # Конфигурация
├── database.py         # Работа с БД
├── models.py           # Модели данных
├── keyboards.py        # Клавиатуры
├── handlers/           # Обработчики разделов
│   ├── movies.py
│   ├── activities.py
│   ├── trips.py
│   ├── tiktok.py
│   ├── photos.py
│   ├── games.py
│   └── sexual.py
├── requirements.txt    # Зависимости
├── Dockerfile          # Образ Docker
├── docker-compose.yml  # Docker Compose
├── deploy.py           # Скрипт развертывания
└── data/               # База данных (создается автоматически)
```

## Управление ботом

### Просмотр логов
```bash
docker-compose logs -f
```

### Остановка бота
```bash
docker-compose down
```

### Перезапуск бота
```bash
docker-compose restart
```

### Обновление бота
```bash
docker-compose down
docker-compose build
docker-compose up -d
```
Или используйте `update.sh`, который выполнит эти шаги автоматически.

## База данных

База данных SQLite хранится в директории `data/multilists.db`. Для резервного копирования просто скопируйте этот файл.

## Работа с версиями

Для управления версиями проекта используйте Git. Создайте репозиторий на GitHub и используйте стандартные команды Git для работы с проектом.

## Безопасность

- Не публикуйте файл `.env` с токеном бота
- Не публикуйте `config.json` с Telegram ID пользователей
- Регулярно делайте резервные копии базы данных
- Файлы `.env` и `config.json` автоматически исключены из Git (см. `.gitignore`)

## Лицензия

Проект создан для личного использования.

