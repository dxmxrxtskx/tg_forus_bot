<!-- 1cd60433-fb43-4c7e-ae19-d5c42ff79e20 5b100bcc-9e46-4114-b22f-e6d47f4dd99f -->
# Подробный план функциональности Telegram Multi-List Bot

## Общая архитектура

### Технологический стек

- **Язык**: Python 3.11+
- **Библиотека Telegram**: python-telegram-bot
- **База данных**: SQLite (файл `data/multilists.db`)
- **Контейнеризация**: Docker + Docker Compose
- **Управление версиями**: Git + GitHub

### Структура проекта

```
FU_bot/
├── bot.py              # Главный файл, инициализация и регистрация обработчиков
├── config.py           # Конфигурация: токен бота, авторизованные пользователи
├── database.py         # Все операции с БД: создание таблиц, CRUD операции
├── keyboards.py        # Построение всех клавиатур (inline и reply)
├── handlers/           # Обработчики для каждого раздела
│   ├── movies.py       # Фильмы
│   ├── activities.py   # Активности
│   ├── trips.py        # Поездки
│   ├── tiktok.py       # Тренды TikTok
│   ├── photos.py       # Фотографии
│   ├── games.py        # Игры
│   └── sexual.py       # Sexual контент
├── deploy.py           # Скрипт автоматического развертывания
└── data/               # Директория для БД (создается автоматически)
```

### Авторизация

- Доступ только для авторизованных пользователей (Telegram ID из `config.json`)
- Минимум 2 пользователя
- Проверка при каждом запросе через `is_authorized_user()`

---

## Главное меню

### Интерфейс

- **Reply Keyboard** (постоянная клавиатура): 7 кнопок разделов
- **Inline Keyboard** (для callback): те же 7 разделов
- Кнопка "Главное меню" доступна во всех разделах

### Разделы

1. Фильмы
2. Активности
3. Поездки
4. Тренды TikTok
5. Фотографии
6. Игры
7. Sexual

---

## Раздел 1: Фильмы

### Структура данных

**Таблица `movies`:**

- `id` - уникальный идентификатор
- `title` - название фильма (обязательно)
- `note` - примечание (опционально)
- `category_id` - ID категории (Фильм/Сериал/Мультик)
- `user1_rating` - оценка первого пользователя (1-10)
- `user2_rating` - оценка второго пользователя (1-10)
- `watched` - флаг просмотра (0/1)
- `created_at` - дата создания

**Таблица `movie_categories`:**

- Дефолтные категории: "Фильм", "Сериал", "Мультик"
- Возможность добавления новых категорий

### Функциональность

#### Меню раздела

- Ожидающие просмотра
- Просмотренные
- Случайный фильм
- Добавить фильм
- Главное меню

#### Ожидающие просмотра

**Подменю:**

- Общий список
- Фильмы (только фильмы)
- Сериалы (только сериалы)
- Мультики (только мультики)
- Назад

**Список:**

- Пагинация (по 10 элементов)
- Кнопка "Назад" для возврата в подменю
- При выборе фильма - детальный просмотр

**Детальный просмотр:**

- Название
- Примечание (если есть)
- Категория
- Кнопки:
  - Просмотрен (если не просмотрен)
  - Редактировать
  - Удалить
  - Назад

#### Просмотренные

**Подменю:**

- Общий список
- Топ-10
- Назад

**Общий список:**

- Все просмотренные фильмы
- Пагинация
- Детальный просмотр с рейтингами

**Топ-10:**

- Общий топ (средний рейтинг двух пользователей)
- Топ пользователя 1 (по имени из config.json)
- Топ пользователя 2 (по имени из config.json)
- Назад

**Формат топа:**

```
🏆 Топ-10:
1. Название фильма - 9.5/10
2. Название фильма - 9.0/10
...
```

#### Случайный фильм

- Выбирает случайный фильм из ожидающих (исключая сериалы)
- Показывает детальный просмотр
- Можно добавить в просмотренные

#### Добавление фильма

**Процесс (ConversationHandler):**

1. Ввод названия
2. Ввод примечания (или /skip)
3. Выбор категории:

   - Существующие категории
   - Создать новую категорию

4. Сохранение

#### Редактирование фильма

**Процесс:**

1. Новое название (или /skip)
2. Новое примечание (или /skip)
3. Сохранение

#### Оценка фильма

**После отметки "Просмотрен":**

1. Оценка пользователя 1 (1-10)
2. Оценка пользователя 2 (1-10)
3. Сохранение

**Клавиатура оценки:**

- Кнопки от 1 до 10
- Отмена

---

## Раздел 2: Активности

### Структура данных

**Таблица `activities`:**

- `id` - уникальный идентификатор
- `title` - название активности (обязательно)
- `note` - примечание (опционально)
- `status` - статус: "planned" или "done"
- `created_at` - дата создания

### Функциональность

#### Меню раздела

- Планируемые
- Выполненные
- Добавить активность
- Главное меню

#### Планируемые активности

- Список всех активностей со статусом "planned"
- Пагинация (по 10 элементов)
- Кнопка "Назад" в меню раздела

**Детальный просмотр:**

- Название
- Примечание (если есть)
- Кнопки:
  - Выполнено
  - Редактировать
  - Удалить
  - Назад (в список планируемых)

#### Выполненные активности

- Список всех активностей со статусом "done"
- Пагинация
- Кнопка "Назад" в меню раздела

**Детальный просмотр:**

- Название
- Примечание (если есть)
- Кнопки:
  - Редактировать
  - Удалить
  - Назад (в список выполненных)

#### Добавление активности

**Процесс:**

1. Ввод названия
2. Ввод примечания (или /skip)
3. Сохранение (по умолчанию статус "planned")

#### Редактирование активности

**Процесс:**

1. Новое название (или /skip)
2. Новое примечание (или /skip)
3. Сохранение

#### Отметка "Выполнено"

- Меняет статус с "planned" на "done"
- Обновляет детальный просмотр
- Кнопка "Выполнено" исчезает

---

## Раздел 3: Поездки

### Структура данных

**Таблица `trips`:**

- `id` - уникальный идентификатор
- `title` - название поездки (обязательно)
- `note` - примечание (опционально)
- `category_id` - ID категории
- `visited` - флаг посещения (0/1)
- `created_at` - дата создания

**Таблица `trip_categories`:**

- Дефолтные категории: "Пешком", "Поездки", "Места в Херцег-Нови"
- Возможность добавления новых категорий

### Функциональность

#### Меню раздела

- Пешком
- Поездки
- Места в Херцег-Нови
- Добавить
- Главное меню

#### Списки по категориям

**Для каждой категории:**

- Список поездок в категории
- Пагинация (по 10 элементов)
- Кнопка "Назад" в список категорий

**Детальный просмотр:**

- Название
- Примечание (если есть)
- Категория
- Кнопки:
  - Посещено (если не посещено)
  - Редактировать
  - Удалить
  - Назад (в список категории)

#### Добавление поездки

**Процесс:**

1. Ввод названия
2. Ввод примечания (или /skip)
3. Выбор категории:

   - Существующие категории
   - Создать новую категорию

4. Сохранение

#### Редактирование поездки

**Процесс:**

1. Новое название (или /skip)
2. Новое примечание (или /skip)
3. Сохранение

#### Отметка "Посещено"

- Меняет `visited` с 0 на 1
- Обновляет детальный просмотр
- Кнопка "Посещено" исчезает

---

## Раздел 4: Тренды TikTok

### Структура данных

**Таблица `tiktok_trends`:**

- `id` - уникальный идентификатор
- `title` - название тренда (обязательно)
- `video_file_id` - ID видео файла в Telegram (опционально)
- `status` - статус: "todo" или "done"
- `created_at` - дата создания

### Функциональность

#### Меню раздела

- Надо снять
- Снятые
- Добавить тренд
- Главное меню

#### Надо снять

- Список трендов со статусом "todo"
- Пагинация
- Кнопка "Назад" в меню раздела

**Детальный просмотр:**

- Название
- Видео (если прикреплено)
- Кнопки:
  - Выполнено
  - Удалить
  - Назад (в список "Надо снять")

#### Снятые

- Список трендов со статусом "done"
- Пагинация
- Кнопка "Назад" в меню раздела

**Детальный просмотр:**

- Название
- Видео (если прикреплено)
- Кнопки:
  - Удалить
  - Назад (в список "Снятые")

#### Добавление тренда

**Процесс:**

1. Ввод названия
2. Прикрепление видео (видео или документ-видео)
3. Сохранение (по умолчанию статус "todo")

**Особенности:**

- Если сообщение с видео удаляется, используется `reply_text` вместо `edit_message_text`

#### Отметка "Выполнено"

- Меняет статус с "todo" на "done"
- Если сообщение содержит видео, используется `reply_text` для нового сообщения

---

## Раздел 5: Фотографии

### Структура данных

**Таблица `photo_categories`:**

- `id` - уникальный идентификатор
- `title` - название категории (обязательно, UNIQUE)
- `link` - ссылка (опционально)
- `description` - описание (опционально)

**Дефолтные категории:**

- "for all"
- "not for all"

### Функциональность

#### Меню раздела

- Список категорий (если есть)
- Добавить категорию
- Главное меню

#### Список категорий

- Все категории фотографий
- Пагинация (по 10 элементов)
- Кнопка "Добавить категорию"
- Кнопка "Главное меню"

**Детальный просмотр категории:**

- Название
- Ссылка (если есть)
- Описание (если есть)
- Кнопки:
  - Редактировать
  - Удалить
  - Назад (в список категорий)

#### Добавление категории

**Процесс:**

1. Ввод названия
2. Ввод ссылки (или /skip)
3. Ввод описания (или /skip)
4. Сохранение

#### Редактирование категории

**Процесс:**

1. Новое название (или /skip)
2. Новая ссылка (или /skip)
3. Новое описание (или /skip)
4. Сохранение

---

## Раздел 6: Игры

### Структура данных

**Таблица `games`:**

- `id` - уникальный идентификатор
- `title` - название игры (обязательно)
- `note` - примечание (опционально)
- `genre` - жанр (опционально)
- `status` - статус: "pending" или "done"
- `user1_rating` - оценка первого пользователя (1-10)
- `user2_rating` - оценка второго пользователя (1-10)
- `created_at` - дата создания

### Функциональность

#### Меню раздела

- Ожидающие
- Пройденные
- Топ-10
- Случайная игра
- Добавить игру
- Главное меню

#### Ожидающие игры

**Подменю:**

- Общий список
- Кнопки для каждого жанра (динамически)
- Назад

**Список:**

- Общий список или фильтр по жанру
- Пагинация
- Кнопка "Назад" в подменю

**Детальный просмотр:**

- Название
- Примечание (если есть)
- Жанр (если есть)
- Кнопки:
  - Пройдено
  - Редактировать
  - Удалить
  - Назад (в список ожидающих)

#### Пройденные игры

**Подменю:**

- Общий список
- Топ-10
- Назад

**Общий список:**

- Все игры со статусом "done"
- Пагинация
- Детальный просмотр с рейтингами

**Топ-10:**

- Общий топ (средний рейтинг)
- Топ пользователя 1
- Топ пользователя 2
- Назад

#### Случайная игра

- Выбирает случайную игру из ожидающих
- Показывает детальный просмотр

#### Добавление игры

**Процесс:**

1. Ввод названия
2. Ввод примечания (или /skip)
3. Ввод жанра (или /skip)
4. Сохранение (по умолчанию статус "pending")

#### Редактирование игры

**Процесс:**

1. Новое название (или /skip)
2. Новое примечание (или /skip)
3. Новый жанр (или /skip)
4. Сохранение

#### Отметка "Пройдено"

**После отметки:**

1. Оценка пользователя 1 (1-10)
2. Оценка пользователя 2 (1-10)
3. Сохранение (статус меняется на "done")

---

## Раздел 7: Sexual

### Структура данных

**Таблица `sexual`:**

- `id` - уникальный идентификатор
- `title` - название (обязательно)
- `link` - ссылка (опционально)
- `description` - описание (опционально)

**Особенность:** Простой список без категорий

### Функциональность

#### Меню раздела

- Список всех записей (если есть)
- Добавить
- Главное меню

#### Список записей

- Все записи
- Пагинация (по 10 элементов)
- Кнопка "Добавить"
- Кнопка "Главное меню"

**Детальный просмотр:**

- Название
- Ссылка (если есть)
- Описание (если есть)
- Кнопки:
  - Редактировать
  - Удалить
  - Назад (в список)

#### Добавление записи

**Процесс:**

1. Ввод названия
2. Ввод ссылки (или /skip)
3. Ввод описания (или /skip)
4. Сохранение

#### Редактирование записи

**Процесс:**

1. Новое название (или /skip)
2. Новая ссылка (или /skip)
3. Новое описание (или /skip)
4. Сохранение

---

## Технические детали

### Обработка ошибок

#### Bad Request (400)

- Игнорируется при попытке редактировать сообщение с тем же содержимым
- Функция `safe_edit_message_text()` в `handlers/trips.py`
- Обработка в TikTok при работе с видео

#### Message is not modified

- Игнорируется при повторных нажатиях кнопок
- Обработка в критических местах

#### There is no text in the message to edit

- В TikTok: используется `reply_text` если сообщение содержит видео
- Обработка в `tiktok_mark_done()`

### Пагинация

- Функция `list_keyboard()` в `keyboards.py`
- По 10 элементов на страницу
- Кнопки "Назад" и "Вперед"
- Поддержка кастомных кнопок "Назад"

### ConversationHandler

- Используется для многошаговых операций (добавление, редактирование)
- Состояния хранятся в `context.user_data`
- Отмена через кнопку "Отмена" или команду `/cancel`

### База данных

- SQLite с `row_factory = sqlite3.Row`
- Все операции через функции в `database.py`
- Автоматическая инициализация при запуске
- Миграции через `ALTER TABLE` с обработкой ошибок

### Конфигурация

- Токен бота из `.env` (переменная `BOT_TOKEN`)
- Пользователи из `config.json`
- Минимум 2 пользователя
- Имена пользователей используются в топах

### Развертывание

- Скрипт `deploy.py` для автоматической настройки
- Docker Compose для контейнеризации
- Автоматическое создание директорий и файлов
- Резервное копирование БД через копирование файла

---

## Навигация

### Кнопки "Назад"

- В каждом разделе возвращают на уровень выше
- В списках - в меню раздела или подменю
- В детальных просмотрах - в соответствующий список

### Кнопки "Главное меню"

- Доступны во всех разделах
- Возвращают в главное меню
- Используют inline keyboard для callback queries

### Приоритет обработчиков

- Главное меню: `group=0` (высокий приоритет)
- Section handler: `group=0`
- Остальные обработчики: без указания группы (низкий приоритет)

---

## Безопасность

### Авторизация

- Проверка Telegram ID при каждом запросе
- Неавторизованные пользователи получают сообщение об отказе

### Конфиденциальные данные

- `.env` и `config.json` в `.gitignore`
- Не публикуются в репозитории
- Резервное копирование БД рекомендуется

---

## Логирование

- Уровень: INFO
- Формат: `%(asctime)s - %(name)s - %(levelname)s - %(message)s`
- Логирование всех операций с БД
- Логирование ошибок в обработчиках