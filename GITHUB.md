# Работа с GitHub

Это руководство поможет настроить проект для работы с GitHub и упростит процесс загрузки и обновления кода.

> **Важно:** Данная инструкция предназначена для работы на **Windows** (локальный компьютер). Для развертывания на сервере Ubuntu см. раздел "Работа с несколькими серверами" ниже.

---

## 1. Первоначальная настройка (один раз)

### Шаг 1: Инициализация Git репозитория

```bash
# Инициализация репозитория
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Telegram Multi-List Bot"
```

### Шаг 2: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name**: `FU_bot` (или любое другое имя)
   - **Description**: "Telegram bot for managing multiple lists"
   - **Visibility**: Private (рекомендуется) или Public
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

### Шаг 3: Подключение локального репозитория к GitHub

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/FU_bot.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/FU_bot.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

---

## 2. Ежедневная работа с проектом

### Загрузка изменений на GitHub

```bash
# Проверить статус изменений
git status

# Добавить все изменения
git add .

# Или добавить конкретные файлы
# git add bot.py handlers/movies.py

# Создать коммит с описанием
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

### Получение обновлений с GitHub

```bash
# Получить изменения с GitHub
git pull

# Если есть конфликты, Git сообщит об этом
```

### Просмотр истории изменений

```bash
# Посмотреть последние коммиты
git log --oneline

# Посмотреть изменения в файлах
git diff
```

---

## 3. Использование скриптов для упрощения

В проекте есть скрипты `git_push.bat` (Windows) и `git_push.sh` (Linux/Mac), которые автоматизируют процесс загрузки.

### Windows:
```bash
.\git_push.bat "Описание изменений"
```

### Linux/Mac:
```bash
chmod +x git_push.sh
./git_push.sh "Описание изменений"
```

---

## 4. Важные файлы, которые НЕ попадают в GitHub

Благодаря `.gitignore`, следующие файлы **не будут** загружены на GitHub:
- `.env` - содержит токен бота (секретный!)
- `config.json` - содержит Telegram ID пользователей (секретный!)
- `data/` - база данных
- `__pycache__/` - кэш Python
- `.venv/` - виртуальное окружение

**⚠️ ВАЖНО:** Никогда не коммитьте файлы `.env` и `config.json` в публичный репозиторий!

---

## 5. Работа с несколькими серверами (Windows → Ubuntu)

Если вы разрабатываете на **Windows**, а бот работает на **сервере Ubuntu**, используйте следующий workflow:

### На локальном компьютере (Windows):
1. Разрабатывайте и тестируйте код локально
2. Загружайте изменения на GitHub:
   ```bash
   git_push.bat "Описание изменений"
   ```

### На сервере Ubuntu (первый раз):
```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip

# Клонируйте репозиторий
cd /opt
sudo git clone https://github.com/YOUR_USERNAME/FU_bot.git
cd FU_bot
sudo chown -R $USER:$USER .

# Настройте конфигурацию
python3 deploy.py
# Или создайте .env и config.json вручную

# Запустите бота
docker compose up -d
```

### Обновление на сервере Ubuntu:
```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Перейдите в директорию проекта
cd /opt/FU_bot

# Получите последние изменения с GitHub
git pull

# Перезапустите контейнер
docker compose restart

# Или полная пересборка (если изменились зависимости)
docker compose down
docker compose build
docker compose up -d
```

### Типичный workflow (Windows → Ubuntu):

1. **На Windows** (разработка):
   ```bash
   # Внесли изменения в код
   git_push.bat "Добавлена новая функция"
   ```

2. **На Ubuntu** (развертывание):
   ```bash
   ssh user@server
   cd /opt/FU_bot
   git pull
   docker compose restart
   ```

> **Совет:** Для автоматизации можно создать скрипт на сервере `update.sh`, который будет выполнять `git pull` и перезапуск контейнера.

---

## 6. Создание тегов для версий

Для отметки важных версий:

```bash
# Создать тег
git tag -a v1.0.0 -m "Версия 1.0.0"

# Отправить теги на GitHub
git push origin v1.0.0

# Или отправить все теги
git push origin --tags
```

---

## 7. Работа с ветками (опционально)

Для разработки новых функций можно использовать ветки:

```bash
# Создать новую ветку
git checkout -b feature/new-feature

# Работать над изменениями...
git add .
git commit -m "Добавлена новая функция"

# Вернуться в main
git checkout main

# Объединить изменения
git merge feature/new-feature

# Удалить ветку
git branch -d feature/new-feature
```

---

## 8. Решение проблем

### Если забыли добавить файл в .gitignore

```bash
# Удалить файл из индекса Git (но оставить на диске)
git rm --cached .env

# Закоммитить изменение
git commit -m "Remove .env from tracking"
git push
```

### Если нужно отменить последний коммит (еще не отправлен)

```bash
# Отменить коммит, но оставить изменения
git reset --soft HEAD~1

# Или полностью отменить изменения
git reset --hard HEAD~1
```

### Если есть конфликты при pull

```bash
# Git покажет файлы с конфликтами
# Откройте их и разрешите конфликты вручную
# Затем:
git add .
git commit -m "Resolve merge conflicts"
git push
```

---

## 9. Быстрая справка команд

| Действие | Команда |
|----------|---------|
| Проверить статус | `git status` |
| Добавить все файлы | `git add .` |
| Создать коммит | `git commit -m "Сообщение"` |
| Отправить на GitHub | `git push` |
| Получить обновления | `git pull` |
| Посмотреть историю | `git log --oneline` |
| Посмотреть изменения | `git diff` |
| Проверить удаленный репозиторий | `git remote -v` |

---

## 10. Рекомендации по коммитам

Хорошие сообщения коммитов:
- ✅ `"Добавлена поддержка рейтингов для игр"`
- ✅ `"Исправлена ошибка в обработке фильмов"`
- ✅ `"Обновлена документация"`

Плохие сообщения коммитов:
- ❌ `"Изменения"`
- ❌ `"fix"`
- ❌ `"asdf"`

**Правило:** Коммит должен описывать **что** было изменено и **зачем**.

---

Теперь вы готовы эффективно работать с GitHub! 🚀

